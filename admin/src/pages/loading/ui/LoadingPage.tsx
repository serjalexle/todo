import { Typography } from "@mui/material";

const LoadingPage = () => {
  return (
    <Typography variant="h3" color="red" mt={2}>
      Loading...
    </Typography>
  );
};

export default LoadingPage;
